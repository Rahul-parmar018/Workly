package com.example.workly.data

import com.google.firebase.firestore.FirebaseFirestore
import java.util.*

object MockDataSeeder {
    fun seedMultiServices() {
        // No mock services or orders are seeded to ensure only real app data is used.
    }

    fun seedMissingCategoriesOnce() {
        // Only seeds basic categories if missing, no orders or service results.
    }
}
